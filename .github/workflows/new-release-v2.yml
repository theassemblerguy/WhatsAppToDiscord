name: Build and Release Binaries

on:
  workflow_dispatch:
    inputs:
      channel:
        description: "Release channel (stable or unstable)"
        required: true
        default: stable
      tag:
        description: "Override the release tag (defaults to package.json version)"
        required: false

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Bundle with esbuild
        run: npm run bundle

      - name: Generate binaries
        run: |
          mkdir -p build
          npx -y @yao-pkg/pkg out.js -t node24-linux-x64   --options no-warnings -o build/WA2DC-Linux
          npx -y @yao-pkg/pkg out.js -t node24-linux-arm64 --options no-warnings --no-bytecode \
                                                      --public --public-packages "*" -o build/WA2DC-Linux-arm64
          npx -y @yao-pkg/pkg out.js -t node24-macos-x64   --options no-warnings -o build/WA2DC-macOS
          npx -y @yao-pkg/pkg out.js -t node24-win-x64     --options no-warnings --no-bytecode \
                                                      --public --public-packages "*" -o build/WA2DC.exe

      - name: Generate checksums
        run: |
          sha256sum build/WA2DC-* > /dev/null   # prime the checksum file names
          sha256sum build/WA2DC-Linux         > build/WA2DC-Linux.sha256
          sha256sum build/WA2DC-Linux-arm64   > build/WA2DC-Linux-arm64.sha256
          sha256sum build/WA2DC-macOS         > build/WA2DC-macOS.sha256
          sha256sum build/WA2DC.exe           > build/WA2DC.exe.sha256

      - name: Sign binaries
        env:
          SIGN_KEY: ${{ secrets.SIGN_KEY }}
        run: |
          echo "$SIGN_KEY" > private.pem
          openssl dgst -sha256 -sign private.pem -out build/WA2DC-Linux.sig        build/WA2DC-Linux
          openssl dgst -sha256 -sign private.pem -out build/WA2DC-Linux-arm64.sig  build/WA2DC-Linux-arm64
          openssl dgst -sha256 -sign private.pem -out build/WA2DC-macOS.sig        build/WA2DC-macOS
          openssl dgst -sha256 -sign private.pem -out build/WA2DC.exe.sig          build/WA2DC.exe
          rm private.pem

      - name: Derive tag
        id: tag
        run: |
          if [ -n "${{ inputs.tag }}" ]; then
            VERSION="${{ inputs.tag }}"
          else
            VERSION=$(jq -r '.version' package.json)
            if [[ -z "$VERSION" || "$VERSION" == "null" ]]; then
              echo "Version not found in package.json" >&2
              exit 1
            fi
            VERSION="v$VERSION"
          fi
          echo "value=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Resolve release flags
        id: channel
        run: |
          CHANNEL=$(echo "${{ inputs.channel }}" | tr '[:upper:]' '[:lower:]')
          if [ "$CHANNEL" != "unstable" ]; then
            CHANNEL="stable"
          fi

          if [ "$CHANNEL" = "unstable" ]; then
            PRERELEASE="true"
            LATEST="false"
          else
            PRERELEASE="false"
            LATEST="true"
          fi

          {
            echo "channel=$CHANNEL"
            echo "prerelease=$PRERELEASE"
            echo "latest=$LATEST"
          } >> "$GITHUB_OUTPUT"

      - name: Create / update GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG="${{ steps.tag.outputs.value }}"
          CHANNEL="${{ steps.channel.outputs.channel }}"
          PRERELEASE="${{ steps.channel.outputs.prerelease }}"
          LATEST="${{ steps.channel.outputs.latest }}"

          ASSETS=(
            build/WA2DC-Linux
            build/WA2DC-Linux.sha256
            build/WA2DC-Linux.sig
            build/WA2DC-Linux-arm64
            build/WA2DC-Linux-arm64.sha256
            build/WA2DC-Linux-arm64.sig
            build/WA2DC-macOS
            build/WA2DC-macOS.sha256
            build/WA2DC-macOS.sig
            build/WA2DC.exe
            build/WA2DC.exe.sha256
            build/WA2DC.exe.sig
          )

          PRERELEASE_FLAG="--prerelease=$PRERELEASE"
          LATEST_FLAG="--latest=$LATEST"

          if gh release view "$TAG" >/dev/null 2>&1; then
            echo "Updating existing release $TAG on channel $CHANNEL…"
            gh release edit "$TAG" "$PRERELEASE_FLAG" "$LATEST_FLAG" --verify-tag --draft=false
            gh release upload "$TAG" "${ASSETS[@]}" --clobber
          else
            echo "Creating new $CHANNEL release $TAG…"
            gh release create "$TAG" --target "${{ github.sha }}" --generate-notes "$PRERELEASE_FLAG" "$LATEST_FLAG" "${ASSETS[@]}"
          fi

          echo "✅ Release ready:"
          gh release view "$TAG" --json url --jq .url
